<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme.Button" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@android:color/holo_red_dark</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:buttonCornerRadius">8dp</item>
    </style>

    <style name="AppTheme.Button.Dark" parent="Widget.AppCompat.Button.Colored">
        <item name="android:colorControlHighlight">@color/colorSecondary</item>
        <item name="android:backgroundTint">@color/loginColor</item>
        <item name="android:textColor">@color/colorSecondary</item>
        <item name="android:gestureColor">@color/colorSecondary</item>
        <item name="android:buttonCornerRadius">8dp</item>
    </style>

    <style name="FactionPopupMenu" parent="@android:style/Widget.PopupMenu">
        <item name="android:popupBackground">@color/loginColor</item>
        <item name="android:textColor">@color/colorSecondary</item>
    </style>
</resources>